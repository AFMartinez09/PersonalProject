import { Request, Response } from "express";



export const getWorkers = ( req:Request, res:Response ) => {
  res.json({
    message: 'getWorkers'
  });
  // try {
    
  // } catch (error) {
    
  // }
}